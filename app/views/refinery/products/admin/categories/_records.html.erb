<%= render 'refinery/admin/search_header', :url => refinery.products_admin_categories_path %>
<div class='pagination_container'>
  <% if @categories.any? %>
    <%= render 'categories' %>
  <% else %>
    <p>
      <% if searching? %>
        <%= t('no_results', :scope => 'refinery.admin.search') %>
      <% else %>
        <strong>
          <%= t('.no_items_yet') %>
        </strong>
      <% end %>
    </p>
  <% end %>
</div>
